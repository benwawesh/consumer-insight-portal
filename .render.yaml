services:
  - type: web
    name: consumer-insight-portal
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn affiliate.wsgi:application
    envVars:
      - key: DB_ENGINE
        value: django.db.backends.postgresql
      - key: DEBUG
        value: "False"